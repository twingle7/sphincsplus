# thesis-notes-stark-v2（M17）

## 1. 版本叙事主线
- `v0/v1`：公开签名原型与过渡证明对象。
- `v2`：`Show_v2 + pi_F_v2` 目标形态，`sigma_com` 不出现在外部证明对象中。

## 2. 关键里程碑对照
- M12：盲签协议层语义冻结（角色、流程、边界）。
- M13：`pi_F_v2` 字节级格式冻结（去除 `sigma_com` 明文）。
- M14：Rust 后端接入 `pi_F_v2` prove/verify 主通路。
- M15：C 侧默认 show 路径切换到 FFI，验证兼容 `v1/v2`。
- M16：盲签端到端工程闭环测试与可链接性工程检查。
- M17：测试矩阵、性能采集、发布检查与论文材料收口。

## 3. 论文图表建议
- 图 1：协议对象演进（`v1 -> v2`）。
- 图 2：生成与验证分派路径（C/Rust 后端）。
- 图 3：三角色交互时序图（User/Signer/Verifier）。
- 表 1：`proof_bytes/prove_ms/verify_ms` 后端对比。
- 表 2：`v1/v2` 兼容矩阵（生成、验证、回放）。

## 4. 边界声明（写作口径）
- 当前 M16 的去盲流程为工程化占位，不等价于最终密码学去盲构造完成。
- 可链接性检查当前为工程断言（基于 `public_ctx` 绑定），不是形式化匿名性证明。
- Rust AIR 与 C `verify_full` 的逐项同语义对齐仍在进行中。

## 5. 发布前必须给出的结论
- `pi_F_v2` 外部对象不泄露 `sigma_com`。
- show 流程可端到端运行，且篡改可检出。
- 至少给出一组可复现 benchmark 数据与测试矩阵结果。
- 三角色交互演示日志可复现，并可映射到协议步骤。
