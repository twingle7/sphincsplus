<img src="https://r2cdn.perplexity.ai/pplx-full-logo-primary-dark%402x.png" style="height:64px;margin-right:32px"/>

# 我想要在第二章2.4.4节加入关于SPHINCS+ 关于消息m double-pass的问题，请你参考这篇文档撰写这一部分，本节参考文献可以独立编号从【1】开始，参考文献使用权威的，适合写入毕业论文引用的，只需要给出2.4.4节即可

好的，现在我有了充足的资料。下面直接给出 2.4.4 节的正文与参考文献。

***

## 2.4.4 消息 M 的双遍处理问题

### 算法流程与双遍处理的成因

SPHINCS+ 标准化为 SLH-DSA（FIPS 205）后，其内部签名算法 `slh_sign_internal`（Algorithm 19）的计算流程揭示了一个关键的工程约束——消息 $M$ 必须被完整读取两次【1】。具体而言，签名过程依次执行以下两步：

**步骤一（随机化值生成）：**

$$
R = \mathrm{PRF\_msg}(\mathrm{SK.prf},\, \mathrm{opt\_rand},\, M)
$$

**步骤二（消息摘要计算）：**

$$
\mathrm{digest} = \mathrm{H\_msg}(R,\, \mathrm{PK.seed},\, \mathrm{PK.root},\, M)
$$

步骤二在计算消息摘要时需要将步骤一得到的 $R$ 作为输入，而 $R$ 本身又依赖完整的消息 $M$，导致 $M$ 在整个签名过程中被完整遍历两次，即所谓的"双遍处理（double-pass）"问题。这一设计与广泛应用于密码学库的 `init-update-final` 流式分块接口（如 EdDSA 实现中同样存在的类似问题）存在根本性兼容障碍，并对大消息场景下嵌入式设备与硬件安全模块（HSM）的内存管理带来显著压力【2】。

### R 对 M 依赖的安全必要性

$R$ 的计算中绑定消息 $M$ 并非偶然的设计选择，而是出于三方面明确的安全考量【1】【2】：

**（1）防止预计算攻击。** $R$ 必须对攻击者不可预测。若 $R$ 的计算中不包含 $M$，则攻击者可在获取目标消息之前预先计算部分签名结构（如超树路径等），为后续选择性伪造提供便利。将 $M$ 绑定至 $R$ 的计算，使得任何预计算必须在知悉完整消息后方能进行。

**（2）确定性模式下的消息区分。** 在确定性签名场景中，`opt_rand` 可固定为公钥种子 $\mathrm{PK.seed}$。若 $R$ 的计算不包含 $M$，则同一密钥对所有消息将共享相同的 $R$，极大削弱 FORS 签名的安全裕度——攻击者只需诱导签名者对若干散列到同一 FORS 叶节点的消息签名，便可通过较低的计算代价重建私钥分量。

**（3）对冲签名设计（Hedged Signing）。** 结合 $\mathrm{SK.prf}$ 的保密性、`opt_rand` 的随机化以及 $M$ 的完整绑定，形成了层次化的安全保障：即便随机数生成器（RNG）存在质量缺陷甚至后门，PRF 对 $M$ 的绑定仍可阻止攻击者在不知晓完整消息的情况下操纵 $R$。

上述设计在密码学上体现了随机化哈希（Randomized Hashing）的核心思想：将签名方案的安全性从依赖哈希函数的完全抗碰撞性（Collision Resistance，CR）降低至更弱的**目标碰撞抵抗性（Target Collision Resistance，TCR）**【3】。Halevi 与 Krawczyk 在 CRYPTO 2006 的奠基性工作中指出，引入签名者选择的随机盐值后，攻击者的碰撞寻找任务从离线模式（不依赖签名者的预计算）转变为在线模式（必须与签名者交互），复杂度大幅提升【3】。SLH-DSA 将 $R$ 绑定至 $M$ 正是这一原则在哈希基签名中的具体实例化：$\mathrm{H\_msg}$ 中包含 $R$ 与 $\mathrm{PK.root}$，使消息摘要的计算同时绑定了签名者身份与随机化值，对外部碰撞攻击具有较强抵御能力。

### Pure 模式与 Pre-Hash 模式

FIPS 205 为 SLH-DSA 定义了两种外部签名接口，两者共享同一内部算法 `slh_sign_internal`，其核心区别在于传递给内部算法的消息构造方式不同【1】。

**Pure SLH-DSA（Algorithm 22）** 直接将原始消息 $M$ 拼接于构造的消息串 $M'$ 中：

$$
M' = \mathrm{toByte}(0, 1) \,\|\, \mathrm{toByte}(|ctx|, 1) \,\|\, ctx \,\|\, M
$$

其中域分离字节 `0x00` 标识 Pure 模式，`ctx` 为上下文字符串。由于 $M$ 作为完整原始消息拼入 $M'$，签名过程不可避免地产生双遍处理问题。Pure 模式的安全分析仅依赖 TCR 而非 CR，是安全假设最为保守的选择【2】。

**HashSLH-DSA（Algorithm 23）** 在传入内部算法前预先对消息进行散列：

$$
M' = \mathrm{toByte}(1, 1) \,\|\, \mathrm{toByte}(|ctx|, 1) \,\|\, ctx \,\|\, \mathrm{OID} \,\|\, \mathrm{PH}(M)
$$

其中 $\mathrm{PH}(M)$ 为消息的预散列值（支持 SHA-256、SHA-512、SHAKE128 及 SHAKE256），`OID` 标识所使用的散列函数。由于 $\mathrm{PH}(M)$ 为固定长度，$M'$ 的长度不再依赖原始消息规模，从而消除了双遍处理问题，与 `init-update-final` 流式接口兼容。然而，HashSLH-DSA 的安全性依赖于预散列函数 $\mathrm{PH}$ 的完全抗碰撞性（CR）——若 $\mathrm{PH}$ 的碰撞被攻破，则可在不同公钥之间复用碰撞对，削弱多用户安全性【1】。

两种模式的主要对比如下表所示：


| 比较维度 | Pure SLH-DSA | HashSLH-DSA |
| :-- | :-- | :-- |
| 消息遍历次数 | 2次（double-pass） | 1次（single-pass） |
| 流式接口兼容 | 不兼容 | 兼容 |
| 安全假设 | TCR（更保守） | CR（更强依赖） |
| 多用户安全 | 强（PK绑定于摘要） | 较弱（碰撞可跨密钥复用） |

### 与本文工作的关联

本文以 Poseidon2 替换 SPHINCS+ 底层哈希函数的核心动机在于提升零知识友好性，这一替换并未改变 `slh_sign_internal` 的双遍处理结构——Poseidon2 同样在步骤一与步骤二中均需完整消费消息 $M$。然而，Poseidon2 的海绵（Sponge）构造天然支持增量式消息吸收（Absorb）操作，在设计层面为将来引入 single-pass 近似方案（如 Pre-Hash 模式）提供了良好的接口兼容性。此外，在面向 STARK 证明的零知识电路设计中，双遍处理意味着电路须对消息 $M$ 的两次哈希计算分别建模，这是本文在算术中间表示（AIR）设计阶段需要显式考量的约束条件。针对受限嵌入式场景的流式内存优化方案可参考 Niederhagen 等人的相关工作【4】。

***

**本节参考文献**

【1】 National Institute of Standards and Technology. (2024). *FIPS 205: Stateless Hash-Based Digital Signature Standard (SLH-DSA)*. NIST. https://doi.org/10.6028/NIST.FIPS.205

【2】 Bernstein, D. J., Hülsing, A., Kölbl, S., Niederhagen, R., Rijneveld, J., \& Schwabe, P. (2019). The SPHINCS+ signature framework. In *Proceedings of the 2019 ACM SIGSAC Conference on Computer and Communications Security (CCS 2019)* (pp. 2129–2146). ACM. https://doi.org/10.1145/3319535.3363229

【3】 Halevi, S., \& Krawczyk, H. (2006). Strengthening digital signatures via randomized hashing. In C. Dwork (Ed.), *Advances in Cryptology – CRYPTO 2006, Lecture Notes in Computer Science*, vol. 4117 (pp. 41–59). Springer. https://doi.org/10.1007/11818175_3

【4】 Niederhagen, R., Roth, J., \& Wälde, J. (2022). Streaming SPHINCS+ for embedded devices using the example of TPMs. In *Progress in Cryptology – AFRICACRYPT 2022, Lecture Notes in Computer Science*, vol. 13377 (pp. 269–291). Springer. https://doi.org/10.1007/978-3-031-17433-9_12
<span style="display:none">[^1][^10][^11][^12][^13][^14][^15][^2][^3][^4][^5][^6][^7][^8][^9]</span>

<div align="center">⁂</div>

[^1]: sphincs_notes.docx

[^2]: https://dl.acm.org/doi/10.1007/11818175_3

[^3]: https://csrc.nist.gov/csrc/media/events/workshop-on-cybersecurity-in-a-post-quantum-world/documents/papers/session5-hulsing-paper.pdf

[^4]: https://csrc.nist.rip/groups/ST/hash/documents/Krawczyk_rhash.pdf

[^5]: https://cseweb.ucsd.edu/~mihir/papers/tcr-hash.pdf

[^6]: https://datatracker.ietf.org/doc/html/draft-irtf-cfrg-rhash-01

[^7]: https://cs.nyu.edu/~dodis/ps/ABR.pdf

[^8]: https://crypto.stanford.edu/firefox-rhash/

[^9]: https://www.ndss-symposium.org/wp-content/uploads/2017/09/transcript-collision-attacks-breaking-authentication-tls-ike-ssh.pdf

[^10]: https://www.w3.org/2007/xmlsec/ws/slides/11-mcintosh-ibm/Overview.pdf

[^11]: https://troll.iis.sinica.edu.tw/pkc16/slides/4-1--Mitigating_Multi-Target-Attacks_inHash-based_Signatures.pdf

[^12]: https://nvlpubs.nist.gov/nistpubs/legacy/sp/nistspecialpublication800-106.pdf

[^13]: https://datatracker.ietf.org/doc/draft-irtf-cfrg-xmss-hash-based-signatures/12/

[^14]: https://www.semanticscholar.org/paper/Strengthening-Digital-Signatures-Via-Randomized-Halevi-Krawczyk/d96113beada7de94dcf1b19afcdebb1dcc87441f

[^15]: https://dblp.org/db/conf/crypto/crypto2006

